//- Links
mixin link(text, url)
  a.link.dark-gray.underline-hover(href='/resume' + url)= text

mixin accent-link(text, url)
  a.link.accent.underline-hover(href='/resume' + url, class=site.printstyles ? 'dark-gray-p' : '')= text

mixin header-link(text, url)
  a.link.bg-accent-text.underline-hover(href='/resume' + url, class=site.printstyles ? 'dark-gray-p' : '')= text
