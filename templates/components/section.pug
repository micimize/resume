//- Section elements
mixin section-title(content)
  h3.section_title.f5.fw7.mt2.mb1.ttu.accent(class=site.printstyles ? 'dark-gray-p' : '')
    if block
      block
    else
      = content

mixin section
  section.cf.mb3.mb2-ns.mb3-p
    block
